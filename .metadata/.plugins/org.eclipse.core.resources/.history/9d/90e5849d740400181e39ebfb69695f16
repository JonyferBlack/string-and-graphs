//============================================================================
// Name        : assign19_mounthly_deals.cpp
// Author      : aivanov
// Version     :
// Copyright   : 
// Description : Hello World in C++, Ansi-style
//============================================================================

#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

void Add(vector<vector<string>>& data, string s, int i){
	data[i-1].push_back(s);
}

void Next(vector<vector<string>>& data, int& currentMounth, const int nextDaysCount){
	string delta;
	vector<string> copy = data;
	if (data.size() > nextDaysCount){
		for (auto i = data.size() - nextDaysCount - 1; i < data.size(); i++){
			delta += " "+ data[i];
		}
		data.resize(nextDaysCount);
		data[nextDaysCount - 1] = " " + delta;
	}
	else {
		data.resize(nextDaysCount);
	}

	currentMounth != 12 ? currentMounth++ : 0;
}

void Dump(const vector<string>& data, const int& i){
	cout << data[i-1].size() << " ";
	for (auto& s : data[i-1]){
		cout << s << " ";
	}
}

int main() {
	const vector<int> dayNumbers = {31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};
	int currentMounth = 0;
	vector<vector<string>> deals(dayNumbers[currentMounth]);

	int Q;
	cin >> Q;

	string command;
	string s;
	int x;
	for (auto i = 0; i < Q; i++){
		cin >> command;
		if (command == "ADD"){
			cin >> x;
			cin >> s;
			Add(deals, s, x);
		}
		if (command == "DUMP"){
			cin >> x;
			Dump(deals, x);
		}
		if (command == "NEXT"){
			Next(deals, currentMounth, dayNumbers[currentMounth + 1]);
		}
	}

	return 0;
}
