#include <iostream>
#include <cmath>
using namespace std;

int main(){
    int a,b,c,D;
    D= b^2-4*a*c;
    if(D>1){
    	cout<<(-b+sqrt(D))/2*a;
    	cout<<(-b-sqrt(D))/2*a;
    	return 0;
    }
    if(D==0){
    	cout<<(-b+sqrt(D))/2*a;
    }

	return 0;
}
